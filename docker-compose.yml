version: '3.8'

services:
  app:
    build: .
    image: xtoxico81/iwannaberich:latest
    container_name: iwannaberich
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data # Persist data locally
    restart: unless-stopped
